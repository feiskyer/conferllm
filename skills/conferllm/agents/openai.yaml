interface:
  display_name: "ConferLLM"
  short_description: "Query configured AI models with persistent chats and built-in host tools"
  default_prompt: "Use $conferllm to consult configured models for specialized reasoning, code reviews, multimodal analysis, or second opinions."
policy:
  allow_implicit_invocation: true
